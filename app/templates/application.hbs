<nav class="navbar">
  <div class="navbar-left">
    <a href="/" class="logo">🚗 Showroom</a>
  </div>

  <div class="navbar-right">
    <a href="/sobre">ℹ️ Sobre Nós</a>

    {{#if this.session.isAuthenticated}}
      <span class="user-name">Olá, {{this.session.currentUser.nome}}</span>
      <button class="logout-button" {{on "click" this.logout}}>Sair</button>
    {{else}}
      <a href="/login">🔐 Login</a>
      <a href="/cadastro">📝 Cadastro</a>
    {{/if}}
  </div>
</nav>


<main>
  {{outlet}}
</main>
